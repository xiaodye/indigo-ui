import { Input } from 'cobalt-ui';
import { FC } from 'react';

const App: FC = () => {
  return <Input showEyeIcon type="password" placeholder="密码输入框" style={{ width: 300 }} />;
};

export default App;
